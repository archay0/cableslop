/**
 * Example library file for the custom coded cables.gl op
 */

// Reusable function for the op
export function exampleFunction(input) {
  // Perform some operation on the input
  const output = input * 2;

  // Return the result
  return output;
}
